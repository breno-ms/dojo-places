CREATE TABLE local (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    code VARCHAR(255),
    neighbourhood VARCHAR(255),
    city VARCHAR(255),
    createdAt TIMESTAMP,
    updatedAt TIMESTAMP
);
